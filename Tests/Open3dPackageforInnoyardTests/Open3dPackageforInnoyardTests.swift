import XCTest
@testable import Open3dPackageforInnoyard

final class Open3dPackageforInnoyardTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(Open3dPackageforInnoyard().text, "Hello, World!")
    }
}
